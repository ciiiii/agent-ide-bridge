{
  "name": "claude-ide-bridge",
  "displayName": "Claude IDE Bridge",
  "description": "Multi-client IDE diff bridge: lets several Claude Code sessions share one code-server window (in-editor diffs, accept/reject). Replaces the single-client official integration.",
  "version": "0.0.11",
  "publisher": "cai",
  "license": "MIT",
  "engines": {
    "vscode": "^1.94.0"
  },
  "categories": [
    "Other"
  ],
  "main": "./dist/extension.js",
  "activationEvents": [
    "onStartupFinished"
  ],
  "contributes": {
    "commands": [
      {
        "command": "claudeIdeBridge.showStatus",
        "title": "Claude IDE Bridge: Show Status"
      },
      {
        "command": "claude-code.acceptProposedDiff",
        "title": "Claude IDE Bridge: Accept Proposed Diff"
      },
      {
        "command": "claude-code.rejectProposedDiff",
        "title": "Claude IDE Bridge: Reject Proposed Diff"
      }
    ],
    "keybindings": [
      {
        "command": "claude-code.acceptProposedDiff",
        "key": "cmd+enter",
        "when": "claudeIdeBridge.diffActive"
      },
      {
        "command": "claude-code.rejectProposedDiff",
        "key": "cmd+backspace",
        "when": "claudeIdeBridge.diffActive"
      }
    ],
    "configuration": {
      "title": "Claude IDE Bridge",
      "properties": {
        "claudeIdeBridge.port": {
          "type": "number",
          "default": 8991,
          "description": "Fixed localhost port for the IDE bridge, kept stable across reloads so sessions can reconnect without re-selecting via /ide. Set 0 for a random port."
        }
      }
    }
  },
  "scripts": {
    "build": "node esbuild.mjs",
    "watch": "node esbuild.mjs --watch",
    "typecheck": "tsc --noEmit",
    "package": "vsce package --no-dependencies -o claude-ide-bridge.vsix"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^20.19.0",
    "@types/vscode": "^1.94.0",
    "@types/ws": "^8.5.12",
    "@vscode/vsce": "^3.2.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0"
  }
}
